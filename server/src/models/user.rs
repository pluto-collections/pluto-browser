#[derive(Debug)]
pub struct User {
    pub id: i32,
    pub username: String,
    pub full_name: String,
    pub email: String,
    pub password: String,
    pub created_at: String,
    pub updated_at: String,
}
